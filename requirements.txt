# Offline NotebookLM for Intel N100 — isolated environment (see setup.sh)
# Broad but mutually compatible pins (LangChain 0.3 + Optimum-Intel + OpenVINO).

langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4

# OpenVINO + model export/quantization
optimum-intel[openvino]>=1.21
openvino>=2024.6
nncf>=2.14            # required for INT4 quantization
transformers>=4.45,<4.49

# Optional web interface
streamlit>=1.40

# Vector store and PDF reading
faiss-cpu>=1.9
pymupdf4llm>=0.0.17   # PDF -> Markdown (tables included), fast and lightweight
pypdf>=5.0            # fallback if PyMuPDF4LLM is unavailable
